{% extends '::base.html.twig' %}

{% block page %}
    <div class="pages">
        <div class="row row-content">
            <div class="col-lg-12 user-menu nopadding">
                {% if app.user %}
                        {{ include(':menus:user_menu.html.twig') }}
                {% endif %}
            </div>
            <div class="col-lg-12">
                {% block content %} {% endblock %}
            </div>
        </div>
    </div>
{% endblock %}    
