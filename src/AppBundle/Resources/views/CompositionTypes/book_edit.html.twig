{% extends 'one_column.html.twig' %}
 
{% block seo %}
    <title> {{ 'composition.title' | trans({'%type%': composition.category.name, '%name%' : composition.title }, 'seo')}}</title>
{% endblock %}

{% block content %}
    <div class="edit-type-page">
        <h1 class="text-center">
            <span class="collor-grey">{{composition.category.name }}</span> 
            <span id="composition-title">"{{ composition.title }}"</span>
        </h1>
        
        {{ form(form) }}
    </div>    
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    
    <script src="{{ asset('vendor/ckeditor/ckeditor.js') }}" type="text/javascript"></script>
{% endblock %}
